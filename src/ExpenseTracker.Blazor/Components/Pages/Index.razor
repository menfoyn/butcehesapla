@page "/"
@inject NavigationManager Nav
@using Microsoft.AspNetCore.Components.Web

<style>
  /* Basit ve uyarısız CSS (Razor  karakterini gerektiren hiçbir kural yok) */
  .hero {
    position: relative;
    border-radius: 20px;
    padding: 64px 28px 56px;
    margin: 28px auto 40px;
    background:
      radial-gradient(1200px 600px at 80% -10%, rgba(99,102,241,.35), transparent 60%),
      radial-gradient(900px 500px at -10% 110%, rgba(16,185,129,.25), transparent 60%),
      linear-gradient(180deg, #0b1220, #0f172a);
    color: #f8fafc;
    overflow: hidden;
  }

  .hero .logo {
    width: 150px;
    height: auto;
    filter: drop-shadow(0 8px 18px rgba(0,0,0,.35));
  }
  .hero h1 {
    font-weight: 800;
    letter-spacing: .3px;
    margin-top: 16px;
    margin-bottom: 10px;
    line-height: 1.15;
    background: linear-gradient(90deg, #ffffff, #dbeafe 45%, #a5b4fc 100%);
    -webkit-background-clip: text;
    background-clip: text;
    color: transparent;
  }
  .hero p.lead {
    color: #e2e8f0;
    max-width: 760px;
    margin: 0 auto;
  }
  .hero-cta .btn {
    padding: 11px 18px;
    border-radius: 12px;
    font-weight: 600;
  }
  .btn-brand {
    background: linear-gradient(90deg, #6366f1, #22c55e);
    color: #fff;
    border: none;
    box-shadow: 0 10px 24px rgba(34,197,94,.28), 0 8px 18px rgba(99,102,241,.22);
    transform: translateZ(0);
    transition: transform .14s ease, box-shadow .14s ease, filter .14s ease;
  }
  .btn-brand:hover {
    transform: translateY(-2px);
    box-shadow: 0 14px 30px rgba(34,197,94,.35), 0 10px 22px rgba(99,102,241,.30);
    filter: brightness(1.05);
  }
  .btn-ghost {
    border: 1px solid rgba(255,255,255,.5);
    color: #fff;
    background: rgba(255,255,255,.06);
    backdrop-filter: blur(6px);
    transition: transform .14s ease, background .14s ease, border-color .14s ease;
  }
  .btn-ghost:hover {
    transform: translateY(-2px);
    background: rgba(255,255,255,.10);
    border-color: rgba(255,255,255,.65);
  }

  .quick-grid { margin-top: 10px; }
  .hover-card {
    border-radius: 16px;
    border: 1px solid #e5e7eb;
    background: linear-gradient(180deg, rgba(255,255,255,.72), rgba(255,255,255,.66));
    backdrop-filter: blur(6px);
    transition: transform .18s ease, box-shadow .18s ease, border-color .18s ease;
    box-shadow: 0 12px 28px rgba(2, 6, 23, .10);
  }
  .hover-card:hover {
    transform: translateY(-4px);
    box-shadow: 0 16px 36px rgba(2, 6, 23, .16);
    border-color: rgba(99,102,241,.35);
  }

  .tile-icon {
    display: inline-flex;
    width: 42px; height: 42px;
    align-items: center; justify-content: center;
    font-size: 22px;
    border-radius: 12px;
    background: #f1f5f9;
  }
  .card-title { font-weight: 700; letter-spacing: .2px; }
  .stretched-link { text-decoration: none; }

  .badge-strip {
    display: flex; gap: 10px; flex-wrap: wrap; margin-top: 10px;
    justify-content: center;
  }
  .pill {
    font-size: .78rem; padding: 5px 12px; border-radius: 999px;
    border: 1px solid rgba(226,232,240,.75);
    color: #475569; background: rgba(248,250,252,.9);
  }

  .quick-actions { margin-top: 18px; }
  .qa {
    display: inline-flex; align-items: center; gap: 8px;
    padding: 8px 12px; border-radius: 999px;
    border: 1px dashed rgba(148,163,184,.55);
    color: #94a3b8; background: rgba(241,245,249,.8);
    transition: transform .15s ease;
    cursor: pointer;
  }
  .qa:hover { transform: translateY(-2px); background: rgba(241,245,249,.95); }
</style>

<!-- HERO -->
<section class="container hero text-center" aria-label="Giriş bölümü">
  <img class="logo" src="images/logo/leptonx/logo-light.png" alt="Expense Reports" />
  <h1>Masraf Takip &amp; Raporlama</h1>
  <p class="lead">Masraf raporlarını yönet, yeni rapor oluştur ve proje bazlı özetlerle daha hızlı karar ver.</p>

  <div class="hero-cta mt-3 d-flex justify-content-center gap-2">
    <button class="btn btn-brand" @onclick="GoToCreateReport" aria-label="Yeni masraf raporu oluştur">➕ Yeni Rapor</button>
    <button class="btn btn-ghost" @onclick="GoToDashboard" aria-label="Gösterge panelini aç">📊 Gösterge Paneli</button>
  </div>

  <div class="badge-strip" role="list" aria-label="Öne çıkan nitelikler">
    <span class="pill" role="listitem">Hızlı</span>
    <span class="pill" role="listitem">Basit</span>
    <span class="pill" role="listitem">Proje Bazlı</span>
  </div>

  <div class="quick-actions d-flex justify-content-center gap-2">
    <button type="button" class="qa" @onclick="GoToExpenseReports" aria-label="Rapor listesine git">🧾 Rapor Listesi</button>
    <button type="button" class="qa" @onclick="GoToCreateReport" aria-label="Yeni rapor oluştur">⚡ Hızlı Oluştur</button>
    <button type="button" class="qa" @onclick="GoToDashboard" aria-label="Grafikleri görüntüle">📈 Haftalık Grafikler</button>
  </div>
</section>

@code {
  private void GoToExpenseReports() => Nav.NavigateTo("/expense-reports");
  private void GoToCreateReport()   => Nav.NavigateTo("/expense-reports/create");
  private void GoToDashboard()      => Nav.NavigateTo("/dashboard");

  private void OnCardKey(KeyboardEventArgs e, Action navigate)
  {
      if (e.Key == "Enter" || e.Key == " ")
      {
          navigate();
      }
  }
}